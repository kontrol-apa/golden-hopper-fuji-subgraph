type Token @entity {
  id: ID!
  ownerString : String!
  owner: Owner!
  goldenHopperStakedForVeFly: Boolean
}

type Owner @entity {
  id: ID!
  tokens: [Token!] @derivedFrom(field: "owner")
  address : Bytes!
  count: Int!
  goldenHopperStakedForVeFly: Boolean
}
